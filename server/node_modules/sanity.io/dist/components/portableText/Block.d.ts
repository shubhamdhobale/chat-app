import type { PortableTextBlock } from "@portabletext/types";
import { ReactNode } from "react";
import * as React from 'react';
type Props = {
    children?: ReactNode;
    node: PortableTextBlock;
};
export default function Block({ children, node }: Props): React.JSX.Element;
export {};
